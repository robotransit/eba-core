# v0.2.0 Implementation Checklist

**Status:** Design fully locked (ADR-020–ADR-037)  
**Implementation state:** Not started  
**Last updated:** 2026-03-17

This checklist maps each locked ADR to concrete implementation tasks.  
It serves as the execution plan for turning the v0.2.0 architecture into working code while preserving all invariants and red lines.

## Recommended Implementation Order

Follow ADR dependency order:

1. Confidence semantics (ADR-021–025)  
2. Memory retrieval (ADR-026–030)  
3. Similarity & optional embeddings (ADR-031–032)  
4. Prompt guardrails (ADR-033)  
5. CI foundation (ADR-034–037)

---

## Confidence Semantics (PR1) — COMPLETE ✅

**Status:** Completed and landed

- [x] Implement confidence update mechanism with EWMA smoothing (ADR-021, ADR-025)  
- [x] Implement critic outcome taxonomy and severity scaling (ADR-022)  
- [x] Implement permission gates and single-cycle failure window (ADR-023)  
- [x] Implement whitelist of admissible input signals (ADR-024)  
- [x] Add deterministic replay tests ensuring identical confidence trajectories under identical critic sequences

**Notes:**  
`ConfidenceSignal` class and full test suite (`tests/test_confidence.py`) have been implemented and verified.  
All invariants and acceptance criteria for PR1 are satisfied.  
Next: PR2 — Memory Retrieval (ADR-026–030)

---

## Memory Retrieval

- [ ] Implement core retrieval contract with stdlib-only deterministic path (ADR-026)  
- [ ] Implement hard enable/disable toggle with atomic fallback (ADR-027)  
- [ ] Implement advisory-only memory context injection with structural separation (ADR-028)  
- [ ] Add structured logging via "eck-core" logger with no behavioral side-effects (ADR-029)  
- [ ] Add tests verifying prompt equivalence across disabled/empty/no-context states (ADR-026–028)  
- [ ] Add full deterministic test suite locking all A2 invariants (ADR-030)  

---

## Similarity & Optional Dependencies

- [ ] Implement similarity retrieval API contract (ADR-031)  
- [ ] Implement optional `eck[embeddings]` extras group and `enable_embeddings` toggle (ADR-032)  
- [ ] Ensure atomic fallback to core path with no prompt drift or core contamination (ADR-032)  

---

## Prompt Guardrails

- [ ] Remove PRIORITIZATION_PROMPT  
- [ ] Enforce no-structured-control-fields guardrail (ADR-033)  
- [ ] Add adversarial tests for structured control field leakage (ADR-033)  

---

## CI Foundation

- [ ] Implement two-layer CI structure (ci-core.yml + ci-capability.yml) (ADR-034–035)  
- [ ] Implement pytest markers, coverage enforcement, and guardrail tests (ADR-036)  
- [ ] Add "eck-core" structured logging and observability artifacts (ADR-037)  
- [ ] Verify (CI-enforced) that core remains strictly stdlib-only and capability layer remains non-blocking  

---

## Acceptance Criteria (Definition of Done)

- All ADRs have corresponding code + tests  
- Core remains strictly stdlib-only (no optional dependencies)  
- All red lines and invariants pass in CI  
- Prompt equivalence, atomic fallback, and guardrails are verified  
- Coverage thresholds are met on all kernel paths  

Once complete, tag v0.2.0 and update status in README.md and ARCHITECTURE.md.

---

Contributions must strictly follow the locked ADRs and red lines.  
See `ARCHITECTURE.md` and `docs/adr/` for the current design baseline.
