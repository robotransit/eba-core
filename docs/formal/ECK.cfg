\* Add statements after this line.
\* ECK.cfg — TLC model configuration for ECK.tla
\* Run: java -cp tla2tools.jar tlc2.TLC ECK.tla -config ECK.cfg -workers auto

CONSTANTS
    NORMAL    = NORMAL
    GUIDED    = GUIDED
    ENFORCED  = ENFORCED
    HALT_MODE = HALT_MODE
    NO_ACTION = NO_ACTION
    PROPOSED  = PROPOSED

INVARIANT
    ECKStateInvariants

PROPERTY
    HaltFreezesKernelState

SPECIFICATION
    Spec
